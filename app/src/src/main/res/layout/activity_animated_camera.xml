<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:ignore="MissingDefaultResource">
    <data>
        <variable name="activity"
            type="com.kt.maps.sample.example.camera.AnimatedCameraActivity"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/activity_random_camera"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.kt.maps.sdk.MapView
            android:id="@+id/map"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            />

        <Button
            android:id="@+id/button_camera_easeTo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/camera_easeTo"
            app:layout_constraintTop_toTopOf="parent"
            />

        <Button
            android:id="@+id/button_camera_flyTo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/camera_flyTo"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toRightOf="@+id/button_camera_easeTo"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>